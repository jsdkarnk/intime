<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="th.ac.kku.jittikonkul.watcharaphon.projecthonghong">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".afterLogin"
            android:label="@string/title_activity_after_login"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".TeachingActivity"
            android:label="@string/txt_teaching"
            android:theme="@style/AppTheme" />
        <activity android:name=".SignupActivity" />
        <activity
            android:name=".Teaching_class_Activity"
            android:label="@string/txt_class_name"
            android:theme="@style/AppTheme" />
        <activity android:name=".membersListview" />
        <activity
            android:name=".afterLogin_class_Activity"
            android:label="@string/title_activity_after_login_class_"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>